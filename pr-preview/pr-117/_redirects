# Cloudflare Pages — URL rewrites & redirects
#
# - apex 與 www 同時可訪問；canonical 統一指向 apex
#   (見 root.tsx + 各頁 meta() 的 <link rel="canonical">)，不做 www → apex 301。
#
# - 未命中路徑的處理：交由 Cloudflare Pages 預設行為。
#   build script 把 dist/client/404/index.html 也複製為 dist/client/404.html，
#   Cloudflare Pages 偵測到 root 404.html 後，會自動以 HTTP 404 狀態回該頁面，
#   不會走 SPA fallback（避免 soft-404）。
#
# 此檔目前刻意保持為空（除註解），未來若需要 split-test 重導或舊網址搬遷時再加。
