# Overview

## What this repository is

- **Public frontend** (Vite + React + TypeScript) for Taiwan **individual income tax** (綜合所得稅) guidance: checklist-style deduction/exemption orientation and optional numeric inputs for rough ordering; summary scenarios compare filing and dividend options using shared domain logic.
- **Not** the private research monorepo; see [`AGENTS.md`](../AGENTS.md).

## Audience and intent

- End users in Taiwan filing context; **UI copy is Traditional Chinese (zh-TW)** in source.
- Informational only; disclaimers in content and export stress non-binding estimates and official e-filing as authority.

## Technical posture

- **Static site (prerendered HTML)**: React Router v7 builds fully static pages to `dist/client/`; no backend in this repo. Checklist inputs and preferences stay in the browser (`localStorage`). Indexable marketing/content routes (`/`, `/about`, `/methodology`, `/deductions/:slug`) ship with per-route SEO metadata. See [`README.md`](../README.md).

## Deployment surfaces

| Surface | Role | Notes |
|---------|------|--------|
| **Production** | Primary public site | [`README.md`](../README.md): Cloudflare Pages; marketing URL `TaiwanTaxCalculate.com`. |
| **GitHub Pages `gh-pages`** | Dev test site + PR previews | Subpaths under org Pages base (e.g. `TaiwanTaxCalculator/dev/`, `TaiwanTaxCalculator/pr-preview/pr-N/`); `VITE_BASE_PATH` injected at build. Root `index.html` on `gh-pages` is generated by [`.github/scripts/render-pages-index.sh`](../.github/scripts/render-pages-index.sh). |

Details: [`14-ci-and-deploy.md`](./14-ci-and-deploy.md).

## Non-goals (current scope)

- No authenticated user accounts, no server-side tax filing, no persistence beyond browser storage.
- The SPA is informational only; it is not a full tax engine or substitute for official e-filing.
- No i18n beyond zh-TW unless explicitly added later.

## Related docs

- Stack: [`02-stack-and-tooling.md`](./02-stack-and-tooling.md)
- Product data and rules: [`05-tax-numbers.md`](./05-tax-numbers.md), [`06-content-modules.md`](./06-content-modules.md), [`08-checklist-engine.md`](./08-checklist-engine.md)
